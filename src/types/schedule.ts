export interface ScheduleMatch {
  home: {
    name: string;
    logo: string;
  };
  away: {
    name: string;
    logo: string;
  };
  date: string;
  time: string;
  stadium: string;
  jornada: string;
}
