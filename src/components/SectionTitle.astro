---
const { title, color = "#c00000" } = Astro.props;
---

<span class="inline-block text-left">
  <span class="relative inline-block magic">
    <span
      class="text-xl font-semibold tracking-tight text-center uppercase magic-text text-pretty md:text-3xl lg:text-left"
      style={`
        background: linear-gradient(to right, ${color}, #ffffff, ${color}, ${color});
        background-size: 200%;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        white-space: nowrap;
      `}
    >
      {title}
    </span>
  </span>
</span>

<style>
  /* Optional: base animation if needed */
  .magic-text {
    animation: background-pan 4s linear infinite;
  }

  @keyframes background-pan {
    0% {
      background-position: 0% center;
    }
    100% {
      background-position: -200% center;
    }
  }
</style>
